name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Hello and thank you for contributing to Diagnostics Framework'
        pr-message: 'Hello and thank you for contributing to Diagnostics Framework'
